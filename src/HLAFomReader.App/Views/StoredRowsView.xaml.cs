using System.Windows.Controls;

namespace HLAFomReader.App.Views;

/// <summary>Stored rows tab: browse the raw registry tables of the two selected FOMs side by side.</summary>
public partial class StoredRowsView : UserControl
{
    public StoredRowsView() => InitializeComponent();
}
